# ToneUp 협업 규칙 (GitHub Flow — 기능별 브랜치)

이 문서는 코딩이 처음이어도 그대로 따라 하면 되도록 만들었습니다.
모르는 게 있으면 혼자 고민하지 말고 팀장에게 바로 물어보세요.

## 기본 규칙

- `main` 브랜치는 항상 실행되는 상태를 유지합니다. `main`에 직접 `push`하지 않습니다.
- 브랜치는 **사람별이 아니라 작업(기능) 단위**로 만듭니다. 브랜치 이름 규칙은 다음과 같습니다.
  - 형식: `feature/<작업내용>`
  - 영어 소문자와 하이픈(`-`)만 사용합니다. 대문자, 띄어쓰기, 한글, 밑줄(`_`)은 쓰지 않습니다.
  - 예: `feature/copy-button`, `feature/mailto-button`, `feature/char-count`
- **PR 하나에는 작업 하나만** 담습니다. 작업하다가 상관없는 다른 것도 고치고 싶어지면, 그건 별도의
  `feature/...` 브랜치와 별도의 PR로 나눕니다. (이유는 [4. PR 만들기](#4-push하고-pr-만들기)에서
  설명합니다.)
- 작업이 병합되면 그 브랜치는 **바로 삭제**하고, 다음 작업은 **항상 `main`에서 새 브랜치를 팝니다.**
  같은 브랜치를 재사용하지 않습니다. (이유는 [6. 병합 후 브랜치 삭제](#6-병합-후-브랜치-삭제)에서
  설명합니다.)
- 모든 변경사항은 PR(Pull Request)을 통해서만 `main`에 들어갑니다.
- PR의 **Merge 버튼은 팀장만** 누릅니다. 팀원은 PR을 만들기만 하면 됩니다.
- **다음 명령어는 이 프로젝트에서 절대 사용하지 않습니다: `rebase`, `push --force`(또는
  `--force-with-lease`), `reset --hard`.** 항상 `merge`와 `pull`만으로 진행합니다. 이 명령어들은
  이미 GitHub에 올라간 커밋 기록을 바꾸거나 지워버릴 수 있어서, 다른 사람의 작업 내용을 실수로
  통째로 날려버릴 위험이 있습니다.

---

## 작업 한 사이클 전체 흐름

작업 하나(=PR 하나)를 시작해서 끝낼 때까지, 아래 6단계를 순서대로 반복합니다.

```
1. main 최신화        git checkout main && git pull origin main
2. 새 브랜치 생성      git checkout -b feature/작업내용
3. 작업 & 커밋         git add . && git commit -m "..."
4. push               git push origin feature/작업내용
5. PR 생성 & 병합      (GitHub에서 PR 생성 → 팀장이 리뷰 후 Merge)
6. 브랜치 삭제         (원격 + 로컬 모두 삭제 후, 다음 작업은 다시 1번부터)
```

아래에서 각 단계를 자세히 설명합니다.

---

## 1. main 최신화

새 작업을 시작하기 전, 매번 아래 순서를 그대로 실행하세요.

```bash
git checkout main
```
`main` 브랜치로 이동합니다.

```bash
git pull origin main
```
다른 사람이 먼저 병합한 최신 내용을 내 컴퓨터로 받아옵니다.

---

## 2. 새 브랜치 만들기

```bash
git checkout -b feature/작업내용
```

`작업내용` 자리에는 이번에 할 일을 영어 소문자와 하이픈으로 짧게 적습니다.

- 예: 복사 버튼을 만든다 → `git checkout -b feature/copy-button`
- 예: 글자 수 표시를 추가한다 → `git checkout -b feature/char-count`
- 예: mailto 버튼을 추가한다 → `git checkout -b feature/mailto-button`

이 브랜치는 반드시 `main`에서 새로 만들어야 합니다. (1번에서 `main`을 최신화한 직후에 바로 이 명령을
실행하는 이유입니다.)

---

## 3. 작업하고 커밋하기

이 브랜치 안에서는 **이번에 정한 작업 하나만** 합니다. 코드를 수정한 뒤, 화면이 잘 뜨는지 확인했다면
아래 순서로 진행합니다.

```bash
git status
```
내가 어떤 파일을 바꿨는지 확인합니다. 혹시 `.env.local`처럼 올리면 안 되는 파일이 목록에 있는지
여기서 꼭 확인하세요.

```bash
git add .
```
바뀐 파일들을 커밋 대상으로 추가합니다.

```bash
git commit -m "무엇을 했는지 짧게 적기"
```
변경사항을 기록으로 남깁니다. 예: `git commit -m "결과 복사 버튼 추가"`

작업 도중 이번 작업과 상관없는 다른 버그나 개선점이 눈에 띄어도, 지금 브랜치에서 같이 고치지
마세요. 메모만 해두고, 나중에 `main`에서 새 `feature/...` 브랜치를 파서 따로 작업합니다.

---

## 4. push하고 PR 만들기

```bash
git push origin feature/작업내용
```
내 브랜치를 GitHub에 올립니다.

푸시가 끝나면 GitHub 저장소 페이지(https://github.com/joheeho/toneup)로 이동하세요.
**"Compare & pull request"** 초록 버튼이 보이면 그걸 눌러서 PR을 만듭니다.
PR을 만들 때 PR 템플릿이 자동으로 나타납니다. 특히 **"이 PR이 해결하는 작업 하나"** 항목에는
이번 브랜치 이름과 똑같은 작업 하나만 적으세요. 만약 여기에 적을 작업이 두 개 이상이라면, PR을
쪼개야 한다는 신호입니다.

> **왜 PR 하나에 작업 하나만 담나요?**
> PR에 여러 작업이 섞이면 리뷰하는 사람이 어디까지가 어떤 목적의 변경인지 구분하기 어렵고,
> 그중 절반만 문제가 있어도 PR 전체가 막힙니다. 작업을 잘게 쪼개면 리뷰도 빠르고, 문제가 생겨도
> 어떤 브랜치 하나만 다시 손보면 되므로 되돌리기도 훨씬 쉽습니다.

PR을 만든 뒤에는 **절대로 스스로 Merge 버튼을 누르지 마세요.** 팀장에게 "PR 올렸어요"라고
알려주면, 팀장이 확인하고 병합합니다.

---

## 5. 병합

팀장이 PR을 확인하고 **"Create a merge commit"** 방식으로 병합합니다. (병합 방식에 대한 자세한
내용은 [팀장 참고](#팀장-참고-병합-담당) 항목을 보세요.)

---

## 6. 병합 후 브랜치 삭제

PR이 병합되면, 그 브랜치는 할 일을 다 한 것입니다. GitHub PR 화면에 보통 병합 직후
**"Delete branch"** 버튼이 나타나는데, 그걸 눌러서 원격 브랜치를 삭제하세요. 그리고 내 컴퓨터에서도
아래 명령으로 로컬 브랜치를 정리합니다.

```bash
git checkout main
git pull origin main
git branch -d feature/작업내용
```

그리고 다음 작업을 시작할 때는 **이 브랜치를 재사용하지 않고**, [1. main 최신화](#1-main-최신화)로
돌아가서 `main`에서 완전히 새로운 `feature/...` 브랜치를 만듭니다.

> **왜 매번 브랜치를 지우고 새로 파나요?**
> 브랜치 하나를 계속 재사용하면, 이미 병합되어 `main`에 들어간 이전 작업의 커밋들이 브랜치 기록에
> 계속 남아있게 됩니다. 그 상태에서 다음 작업을 이어서 하면 PR을 만들 때 "이번에 새로 바뀐 부분"과
> "이미 병합된 예전 변경사항"이 뒤섞여 보여서, 리뷰하는 사람이 무엇이 진짜 새 변경사항인지 알기
> 어려워집니다. 또한 브랜치 이름(`feature/copy-button` 등)이 그 브랜치가 무슨 작업이었는지 그대로
> 보여주는 기록 역할을 하는데, 재사용하면 이름과 실제 내용이 맞지 않게 됩니다. 매번 `main`에서 새로
> 시작하면 항상 최신 상태에서, 딱 그 작업의 변경사항만 깨끗하게 담긴 브랜치로 작업하게 됩니다.

---

## 7. 충돌(Conflict)이 났을 때

`git merge main`이나 `git pull` 중에 아래처럼 빨간 글씨로 `CONFLICT`가 뜰 수 있습니다.

```
CONFLICT (content): Merge conflict in src/App.jsx
```

이때 지켜야 할 규칙은 딱 하나입니다.

> **절대 혼자 해결하려고 하지 마세요. 바로 팀장에게 알리세요.**

- 화면에 뜬 내용을 그대로 캡처하거나 복사해서 팀장에게 보내주세요.
- `git status`를 실행해서 나온 결과도 같이 보내주면 좋습니다.
- 충돌 상태에서 임의로 파일을 지우거나 `git commit`을 하지 마세요. 그대로 두고 기다려주세요.
- 팀장이 화면 공유나 직접 확인을 통해 같이 해결합니다.
- 절대로 `rebase`, `push --force`, `reset --hard`로 "그냥 해결"하려고 하지 마세요. 기본 규칙에서
  금지한 이유가 바로 이런 상황입니다.

---

## 8. PR 만들기 전 체크리스트

PR을 만들기 직전에 아래 항목을 스스로 확인하세요. (PR 템플릿에도 같은 체크박스가 있습니다)

- [ ] 이 PR은 작업 하나만 담고 있다 (상관없는 다른 수정이 섞여 있지 않다)
- [ ] `npm run dev`로 로컬에서 직접 실행해서 화면이 정상적으로 뜨는 것을 확인했다
- [ ] `git status`로 확인했을 때 `.env.local` 파일이 목록에 없다 (API 키 파일은 절대 올리지 않기)
- [ ] 커밋 메시지만 봐도 무엇을 했는지 알 수 있다

---

## 9. 실수했을 때 대처법

누구나 실수할 수 있습니다. 중요한 건 **혼자 해결하려고 하지 않는 것**입니다.
아래 상황이 생기면 무엇을 했는지 그대로, 빠르게 팀장에게 알려주세요.

### `main`에 실수로 직접 push했을 때

```bash
git status
```
지금 상태를 확인만 하고, **그 다음 명령어는 실행하지 마세요.**

- 당황해서 되돌리려고 이것저것 명령어를 치지 마세요. 특히 `reset --hard`나 `push --force`는
  절대 시도하지 마세요. 상황이 더 꼬일 수 있습니다.
- 화면을 그대로 캡처해서 팀장에게 바로 보내주세요.
- `main`을 원래 상태로 되돌리는 작업은 팀장이 직접 처리합니다.

### API 키 같은 민감한 정보를 실수로 커밋했을 때

- push하기 전이라면: 팀장에게 알리고, 팀장 안내에 따라 커밋을 취소합니다.
- **이미 push까지 했다면**: 커밋 기록에서 파일만 지워도 키는 이미 외부에 노출된 것으로 간주해야 합니다.
  가장 먼저 할 일은 파일을 지우는 게 아니라, **그 키를 발급받은 곳(Google AI Studio 등)에서
  키를 폐기하고 새로 발급받는 것**입니다.
- 이 두 경우 모두 팀장에게 즉시 알리는 것이 최우선입니다.

### 그 외에도 뭔가 잘못된 것 같을 때

- "잘 모르겠는데 뭔가 이상해요"도 좋은 신호입니다. 더 진행하지 말고 지금 상태 그대로
  팀장에게 알려주세요. 늦게 알리는 것보다 훨씬 낫습니다.

---

## 팀장 참고 (병합 담당)

- PR을 병합할 때는 **"Create a merge commit"** 옵션을 사용하세요. "Squash and merge"나
  "Rebase and merge"는 사용하지 않습니다. (커밋 기록을 그대로 유지하기 위함)
- 병합 전 PR이 정말 작업 하나만 담고 있는지, PR 템플릿의 "이 PR이 해결하는 작업 하나" 항목이
  명확한지 확인하세요. 여러 작업이 섞여 있다면 쪼개서 다시 올리도록 안내하세요.
- 병합 전 PR 템플릿의 체크박스가 모두 체크되어 있는지 확인하세요.
- 병합 후에는 GitHub의 "Delete branch" 버튼으로 원격 브랜치를 삭제하세요.
