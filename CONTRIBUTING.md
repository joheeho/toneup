# ToneUp 협업 규칙 (GitHub Flow)

이 문서는 코딩이 처음이어도 그대로 따라 하면 되도록 만들었습니다.
모르는 게 있으면 혼자 고민하지 말고 팀장에게 바로 물어보세요.

## 기본 규칙

- `main` 브랜치는 항상 실행되는 상태를 유지합니다. `main`에 직접 `push`하지 않습니다.
- 각자 자기 브랜치(`feat-a`, `feat-b`)에서만 작업합니다. 기능별이 아니라 **사람별** 브랜치입니다.
- 모든 변경사항은 PR(Pull Request)을 통해서만 `main`에 들어갑니다.
- PR의 **Merge 버튼은 팀장만** 누릅니다. 팀원은 PR을 만들기만 하면 됩니다.
- `rebase`, `cherry-pick`, `squash` 같은 명령은 이 프로젝트에서 쓰지 않습니다. `merge`와 `pull`만으로 진행합니다.

---

## 1. 하루 작업을 시작할 때

컴퓨터를 켜고 작업을 시작하기 전, 매번 아래 순서를 그대로 실행하세요.

```bash
git checkout main
```
`main` 브랜치로 이동합니다.

```bash
git pull origin main
```
다른 사람이 어제 병합한 최신 내용을 내 컴퓨터로 받아옵니다.

```bash
git checkout feat-a
```
내 브랜치로 이동합니다. (담당이 B라면 `feat-b`)

```bash
git merge main
```
방금 받아온 `main`의 최신 내용을 내 브랜치에 합칩니다. 이 과정을 매일 아침 해두면
나중에 PR을 만들 때 충돌이 날 확률이 크게 줄어듭니다.

> 만약 `git merge main`을 실행했을 때 `CONFLICT`라는 빨간 글씨가 뜨면,
> 아래 [충돌이 났을 때](#3-충돌conflict이-났을-때) 항목을 바로 확인하세요.

---

## 2. 작업 후 push하고 PR 만들기

코드를 수정한 뒤, 화면이 잘 뜨는지 확인했다면 아래 순서로 진행합니다.

```bash
git status
```
내가 어떤 파일을 바꿨는지 확인합니다. 혹시 `.env.local`처럼 올리면 안 되는 파일이
목록에 있는지 여기서 꼭 확인하세요.

```bash
git add .
```
바뀐 파일들을 커밋 대상으로 추가합니다.

```bash
git commit -m "무엇을 했는지 짧게 적기"
```
변경사항을 기록으로 남깁니다. 예: `git commit -m "입력창 UI 수정"`

```bash
git push origin feat-a
```
내 브랜치를 GitHub에 올립니다. (담당이 B라면 `feat-b`)

푸시가 끝나면 GitHub 저장소 페이지(https://github.com/joheeho/toneup)로 이동하세요.
**"Compare & pull request"** 초록 버튼이 보이면 그걸 눌러서 PR을 만듭니다.
PR을 만들 때 아래 PR 템플릿의 항목을 채우면 됩니다 (자동으로 나타납니다).

PR을 만든 뒤에는 **절대로 스스로 Merge 버튼을 누르지 마세요.** 팀장에게 "PR 올렸어요"라고
알려주면, 팀장이 확인하고 병합합니다.

---

## 3. 충돌(Conflict)이 났을 때

`git merge main`이나 `git pull` 중에 아래처럼 빨간 글씨로 `CONFLICT`가 뜰 수 있습니다.

```
CONFLICT (content): Merge conflict in src/App.jsx
```

이때 지켜야 할 규칙은 딱 하나입니다.

> **절대 혼자 해결하려고 하지 마세요. 바로 팀장에게 알리세요.**

- 화면에 뜬 내용을 그대로 캡처하거나 복사해서 팀장에게 보내주세요.
- `git status`를 실행해서 나온 결과도 같이 보내주면 좋습니다.
- 충돌 상태에서 임의로 파일을 지우거나 `git commit`을 하지 마세요. 그대로 두고 기다려주세요.
- 팀장이 화면 공유나 직접 확인을 통해 같이 해결합니다.

---

## 4. PR 만들기 전 체크리스트

PR을 만들기 직전에 아래 항목을 스스로 확인하세요. (PR 템플릿에도 같은 체크박스가 있습니다)

- [ ] `npm run dev`로 로컬에서 직접 실행해서 화면이 정상적으로 뜨는 것을 확인했다
- [ ] `git status`로 확인했을 때 `.env.local` 파일이 목록에 없다 (API 키 파일은 절대 올리지 않기)
- [ ] 내가 맡은 파일만 수정했고, 다른 사람 담당 파일은 건드리지 않았다
- [ ] 커밋 메시지만 봐도 무엇을 했는지 알 수 있다

---

## 5. 실수했을 때 대처법

누구나 실수할 수 있습니다. 중요한 건 **혼자 해결하려고 하지 않는 것**입니다.
아래 상황이 생기면 무엇을 했는지 그대로, 빠르게 팀장에게 알려주세요.

### `main`에 실수로 직접 push했을 때

```bash
git status
```
지금 상태를 확인만 하고, **그 다음 명령어는 실행하지 마세요.**

- 당황해서 되돌리려고 이것저것 명령어를 치지 마세요. 상황이 더 꼬일 수 있습니다.
- 화면을 그대로 캡처해서 팀장에게 바로 보내주세요.
- `main`을 원래 상태로 되돌리는 작업은 팀장이 직접 처리합니다.

### API 키 같은 민감한 정보를 실수로 커밋했을 때

- push하기 전이라면: 팀장에게 알리고, 팀장 안내에 따라 커밋을 취소합니다.
- **이미 push까지 했다면**: 커밋 기록에서 파일만 지워도 키는 이미 외부에 노출된 것으로 간주해야 합니다.
  가장 먼저 할 일은 파일을 지우는 게 아니라, **그 키를 발급받은 곳(Google AI Studio 등)에서
  키를 폐기하고 새로 발급받는 것**입니다.
- 이 두 경우 모두 팀장에게 즉시 알리는 것이 최우선입니다.

### 그 외에도 뭔가 잘못된 것 같을 때

- "잘 모르겠는데 뭔가 이상해요"도 좋은 신호입니다. 더 진행하지 말고 지금 상태 그대로
  팀장에게 알려주세요. 늦게 알리는 것보다 훨씬 낫습니다.

---

## 팀장 참고 (병합 담당)

- PR을 병합할 때는 **"Create a merge commit"** 옵션을 사용하세요. "Squash and merge"나
  "Rebase and merge"는 사용하지 않습니다. (커밋 기록을 그대로 유지하기 위함)
- 병합 전 PR 템플릿의 체크박스가 모두 체크되어 있는지 확인하세요.
